const commonUrl = '/US/en/1/';

const PageUrls = {
  HomePage: commonUrl + 'home',
  LoginPage: commonUrl + 'login',
  RegisterPage: commonUrl + 'register',
  SettingsPage: commonUrl + 'settings',
  ForumPage: commonUrl + 'forums',
};

export default PageUrls;
